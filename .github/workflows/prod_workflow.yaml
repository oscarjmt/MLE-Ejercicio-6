name: Prod Script Workflow

on:
  push:
    paths:
      - 'data/test.csv'

jobs:
  production:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'  # Configura la versión de Python que necesitas

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run production script
      run: |
        python script_prod.py

    - name: Add changes to git
      run: |
        git config --local user.email "osjomete@gmail.com"
        git config --local user.name "Github Actions"
        git add .
        git commit -m "Updated results after data/test.csv change"
        git push
